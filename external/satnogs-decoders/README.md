This directory is intentionally empty in the downloadable archive.

After placing this project in a Git repository, add the SatNOGS decoders repository as a submodule:

  git submodule add https://gitlab.com/librespacefoundation/satnogs/satnogs-decoders.git external/satnogs-decoders
  git submodule update --init --recursive
